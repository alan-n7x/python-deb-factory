# Scripts

This directory contains utility scripts for the project, such as:

- `release.sh` – automates version bump, changelog generation, building, and publishing.
- `build-deb.sh` – builds the Debian package locally.
- `test.sh` – runs tests and linting.

Feel free to add more helper scripts as needed.